---
layout: layout.njk
title: Keywords Index Page
# keywordsSlugBase is set globally in eleventy.ocnfig.js
---

<h1>{{ title }}</h1>

{% for keywordObj, posts in collections.keywordsAndPosts %}
  <h2><a href="/{{ keywordsSlugBase }}/{{ keywordObj.slug }}/">{{ keywordObj.title }}</a></h2>
  <ul>
  {% for post in posts %}
    <li>
      <a href="{{ post.page.url }}">{% if post.data.title %}{{ post.data.title }}{% else %}***TITLE MISSING***{% endif %}</a>
    </li>
  {% endfor %}
  </ul>
{% endfor %}